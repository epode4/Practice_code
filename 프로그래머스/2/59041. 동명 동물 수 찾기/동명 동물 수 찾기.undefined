-- 코드를 입력하세요
# SELECT NAME, COUNT(*)
# FROM ANIMAL_INS 
# WHERE NAME IS NOT NULL
#     # AND COUNT(*) >= 2
# GROUP BY NAME
# ORDER BY NAME

SELECT NAME, A.COUNT
FROM (
    SELECT *, COUNT(*) AS COUNT
    FROM ANIMAL_INS
    WHERE NAME IS NOT NULL
    GROUP BY NAME   
) A
WHERE A.COUNT >=2
ORDER BY NAME